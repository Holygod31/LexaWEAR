<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?android:attr/colorBackground">

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Write Tag"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="?attr/colorOnBackground"
        android:layout_margin="16dp" />

    <!-- Status -->
    <TextView
        android:id="@+id/tv_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Let's tag a clothing item."
        android:textSize="14sp"
        android:textColor="?attr/colorOnSurface"
        android:gravity="center"
        android:padding="8dp"
        android:background="@drawable/status_background"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:accessibilityLiveRegion="polite" />

    <!-- Step indicator -->
    <TextView
        android:id="@+id/tv_step_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Step 1 of 14"
        android:textSize="12sp"
        android:textColor="?attr/colorOnSurface"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="4dp" />

    <!-- Question -->
    <TextView
        android:id="@+id/tv_question"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="What is this item called?"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="?attr/colorOnBackground"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:accessibilityLiveRegion="polite" />

    <!-- Input field + mic button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="8dp"
        android:gravity="center_vertical">

        <EditText
            android:id="@+id/et_step_input"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="1"
            android:textSize="16sp"
            android:padding="12dp"
            android:background="@drawable/input_background"
            android:inputType="textCapSentences"
            android:contentDescription="Answer input field" />

        <Button
            android:id="@+id/btn_mic"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginStart="8dp"
            android:text="🎤"
            android:textSize="20sp"
            android:padding="0dp"
            android:contentDescription="Tap to speak your answer" />

        <Button
            android:id="@+id/btn_camera"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginStart="8dp"
            android:text="📷"
            android:textSize="20sp"
            android:padding="0dp"
            android:contentDescription="Tap to scan clothing with camera and pre-fill fields" />

    </LinearLayout>

    <!-- Inline error message -->
    <TextView
        android:id="@+id/tv_step_error"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="#D32F2F"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="4dp"
        android:visibility="gone"
        android:accessibilityLiveRegion="polite" />

    <!-- Show all options button — visible only after a rejection on fields with many options -->
    <Button
        android:id="@+id/btn_show_options"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="64dp"
        android:text="Show all options"
        android:textSize="15sp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="8dp"
        android:visibility="gone"
        android:gravity="start|center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:contentDescription="Show all options. Double tap to expand full list." />

    <!-- Navigation buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp">

        <Button
            android:id="@+id/btn_back"
            android:layout_width="0dp"
            android:layout_height="64dp"
            android:layout_weight="1"
            android:text="Back"
            android:textSize="16sp"
            android:layout_marginEnd="4dp"
            android:contentDescription="Go back to previous field" />

        <Button
            android:id="@+id/btn_skip"
            android:layout_width="0dp"
            android:layout_height="64dp"
            android:layout_weight="1"
            android:text="Skip"
            android:textSize="16sp"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:contentDescription="Skip this question" />

        <Button
            android:id="@+id/btn_next"
            android:layout_width="0dp"
            android:layout_height="64dp"
            android:layout_weight="1"
            android:text="Next"
            android:textSize="16sp"
            android:layout_marginStart="4dp"
            android:contentDescription="Go to next field" />

    </LinearLayout>

    <!-- Summary screen (hidden until final step) -->
    <ScrollView
        android:id="@+id/layout_summary"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/tv_summary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textColor="?attr/colorOnBackground"
            android:lineSpacingExtra="6dp"
            android:contentDescription="Summary of entered clothing information"
            android:accessibilityLiveRegion="polite" />

    </ScrollView>

    <!-- Write button pinned to bottom -->
    <Button
        android:id="@+id/btn_write"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:text="Write to Tag"
        android:textSize="18sp"
        android:layout_margin="16dp"
        android:visibility="gone"
        android:contentDescription="Write entered information to NFC tag" />

</LinearLayout>